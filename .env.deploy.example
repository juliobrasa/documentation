# Configuración de Deployment - Documentación
# Copia este archivo a .env.deploy y configura según tu entorno

# Entorno de deployment (production, staging)
DEPLOY_ENVIRONMENT=staging

# Rama Git a desplegar
DEPLOY_BRANCH=main

# Directorio de instalación
DEPLOY_INSTALL_DIR=/var/www/docs

# URL del repositorio Git
DEPLOY_GIT_REPO=https://github.com/juliobrasa/documentation.git

# Servidor web (apache, nginx, none)
DEPLOY_WEB_SERVER=apache

# Puerto del servidor web
DEPLOY_WEB_PORT=8080

# Usuario y grupo del sistema
DEPLOY_USER=www-data
DEPLOY_GROUP=www-data

# Directorio de backups
DEPLOY_BACKUP_DIR=/var/backups/docs

# Archivo de log
DEPLOY_LOG_FILE=/var/log/deploy-docs.log

# Configuración específica por entorno
# Production
PRODUCTION_INSTALL_DIR=/var/www/docs-production
PRODUCTION_BRANCH=main
PRODUCTION_WEB_PORT=8080

# Staging
STAGING_INSTALL_DIR=/var/www/docs-staging
STAGING_BRANCH=develop
STAGING_WEB_PORT=8081
